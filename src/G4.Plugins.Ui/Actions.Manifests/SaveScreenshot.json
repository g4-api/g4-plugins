{
	"aliases": [
		"GetScreenshot",
		"TakeScreenshot"
	],
	"author": {
		"link": "https://www.linkedin.com/in/roei-sabag-247aa18/",
		"name": "Roei Sabag"
	},
	"categories": [
		"WebDriver"
	],
	"context": {
		"integration": {
			"github": {
				"document": "https://github.com/g4-api/g4-plugins/blob/main/docs/Actions/SaveScreenshot.md",
				"source": "https://github.com/g4-api/g4-plugins/blob/main/src/G4.Plugins.Ui/Actions/SaveScreenshot.cs"
			},
			"rag": {
				"description": "The SaveScreenshot action (aliases: GetScreenshot, TakeScreenshot) captures a PNG screenshot using the W3C WebDriver GET /session/{session id}/screenshot command. It supports full-page viewport captures and element-level screenshots when OnElement and Locator are supplied. By default the screenshot is saved to disk: the target directory is created automatically via Directory.CreateDirectory including any missing parent directories, the file name defaults to a new GUID and always receives a .png extension if not already present (case-insensitive), and the saved absolute path is the recorded value. When the optional Base64 switch is supplied (e.g. {{$ --Base64}}) no file is written; instead the screenshot is encoded as a base64 PNG string and that string becomes the recorded value. The plugin is self-contained and decides between disk and base64 output solely from its own Base64 switch, independent of the automation-wide screenshot settings. If a locator is supplied but GetElement returns null the plugin falls back to a full-page screenshot unconditionally. The recorded value (path or base64) is appended to the SaveScreenshot:Screenshots session parameter, accumulating across all invocations in the session, and is also exposed as the Screenshot key on the plugin response entity.",
				"qa": [
					{
						"question": "What does the SaveScreenshot plugin do?",
						"answer": "It captures a PNG screenshot of either the full browser viewport or a specific element using the W3C WebDriver screenshot command. By default it saves the image to disk (creating the output directory if absent) and records the file path; when the Base64 switch is supplied it skips the disk write and records the screenshot as a base64 PNG string instead. The recorded value is appended to the SaveScreenshot:Screenshots session parameter and exposed as the Screenshot key on the plugin response entity. The action is also known by aliases GetScreenshot and TakeScreenshot."
					},
					{
						"question": "What parameters does SaveScreenshot accept?",
						"answer": "It accepts three optional parameters: Directory (the folder to save the screenshot into, defaulting to Environment.CurrentDirectory), FileName (the file name, defaulting to a new GUID string with .png appended automatically when the name does not already end with .png), and Base64 (a switch that, when present, returns the screenshot as a base64 PNG string instead of writing a file to disk)."
					},
					{
						"question": "How does SaveScreenshot choose between a full-page and an element screenshot?",
						"answer": "When OnElement and Locator are provided the plugin calls this.GetElement(rule, element) to resolve the target. If that returns a non-null element it calls element.SaveScreenshot to scope the capture. If GetElement returns null — either because OnElement was absent or the element was not found — the plugin calls WebDriver.SaveScreenshot to capture the full browser viewport. The fallback is unconditional and silent; no error is raised."
					},
					{
						"question": "What is the SaveScreenshot:Screenshots output parameter?",
						"answer": "SaveScreenshot:Screenshots is a session parameter holding a JSON array of the recorded values for every screenshot captured during the session. Each value is an absolute file path by default, or a base64 PNG string when the Base64 switch is used. Each invocation deserializes the existing list, appends the new value, and re-serializes it, so the parameter accumulates values across multiple calls rather than replacing them."
					},
					{
						"question": "How is the file name determined and what extension is enforced?",
						"answer": "The FileName parameter value is used when supplied. If omitted a new GUID string is generated as the default name. In both cases the plugin appends .png if the name does not already end with .png (case-insensitive check via String.EndsWith), ensuring every saved file is a valid PNG regardless of what the caller provides."
					},
					{
						"question": "What happens when the specified Directory does not exist?",
						"answer": "The plugin calls Directory.CreateDirectory before writing the file. This creates the full directory path including any missing parent directories automatically. No error is raised for a new or previously absent directory."
					},
					{
						"question": "What is the manifest version for SaveScreenshot?",
						"answer": "The manifest version is 5."
					},
					{
						"question": "What does the Base64 switch do?",
						"answer": "When the Base64 switch is supplied (e.g. {{$ --Base64}}) the plugin skips writing a file to disk and instead encodes the screenshot as a base64 PNG string. That base64 string becomes the recorded value: it is appended to the SaveScreenshot:Screenshots session parameter and exposed as the Screenshot key on the plugin response entity. This is designed to embed the image inside the response for later processing, so downstream plugins can read it directly from the response without relying on the file system. The switch is self-contained and does not depend on the automation-wide screenshot settings; when omitted the screenshot is saved to disk and the file path is recorded instead."
					},
					{
						"question": "What aliases are available for SaveScreenshot?",
						"answer": "SaveScreenshot is also accepted as GetScreenshot and TakeScreenshot — all three resolve to identical behavior."
					}
				]
			},
			"sequentialWorkflow": {
				"$type": "Action",
				"componentType": "task",
				"iconProvider": {
					"name": "image",
					"svg": null
				},
				"label": "Save Screenshot",
				"model": "ActionRuleModel"
			},
			"mcp": {
				"title": "Save Screenshot"
			}
		}
	},
	"description": [
		"### Purpose",
		"",
		"Captures and saves a PNG screenshot of the browser viewport or a specific element during automation workflows, using the W3C WebDriver `GET /session/{session id}/screenshot` command as its underlying mechanism.",
		"It automatically creates the target directory, enforces the `.png` extension on every output file, generates a unique GUID-based file name when none is provided, and accumulates every saved absolute path in the `SaveScreenshot:Screenshots` session parameter for downstream use.",
		"",
		"### Key Features and Functionality",
		"",
		"| Feature                 | Description                                                                                                                                                    |",
		"|-------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------|",
		"| Full-Page Screenshot    | Captures the entire browser viewport as a PNG file when no target element is specified.                                                                        |",
		"| Element Screenshot      | Captures only the specified element when OnElement and Locator are supplied, using the element-level screenshot call.                                          |",
		"| Null-Element Fallback   | Falls back to a full-page screenshot when a locator is supplied but GetElement returns null, making the action unconditionally safe to invoke.                 |",
		"| Auto Directory Creation | Calls Directory.CreateDirectory before writing, creating the target directory and any missing parent directories automatically.                                |",
		"| Auto File Naming        | Generates a unique GUID-based file name when FileName is omitted, preventing accidental overwrites across repeated invocations.                               |",
		"| PNG Enforcement         | Always appends .png to the output file name if it does not already end with .png (case-insensitive), ensuring every saved file is a valid PNG.                |",
		"| Session Output          | Appends each recorded value (file path, or base64 string when Base64 is used) to the SaveScreenshot:Screenshots session parameter, accumulating a complete list across all invocations in the session. |",
		"| Base64 Embedding        | When the Base64 switch is supplied, skips the disk write and records the screenshot as a base64 PNG string, embedding the image in the response via the Screenshot response entity key for downstream processing.            |",
		"",
		"### Usages in RPA",
		"",
		"| Use Case              | Description                                                                                                                               |",
		"|-----------------------|-------------------------------------------------------------------------------------------------------------------------------------------|",
		"| Audit Trail           | Capture screenshots at key workflow steps to create a visual record for compliance review and process documentation.                      |",
		"| Error Documentation   | Take a screenshot when an unexpected state is detected to provide visual evidence that pinpoints the exact failure point for analysis.     |",
		"| Step Evidence Capture | Save a screenshot after a critical form submission or navigation to confirm the application transitioned to the expected state.            |",
		"",
		"### Usages in Automation Testing",
		"",
		"| Use Case                   | Description                                                                                                                                            |",
		"|----------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------|",
		"| Visual Regression Testing  | Capture screenshots at fixed checkpoints across test runs and compare the accumulated SaveScreenshot:Screenshots paths to detect unintended UI changes. |",
		"| Cross-Browser Verification | Save viewport and element screenshots across browser configurations to confirm consistent rendering and layout behaviour.                              |",
		"| Failure Evidence           | Collect a screenshot immediately at the point of assertion failure to provide a reproducible visual artifact for root-cause analysis.                  |"
	],
	"entity": [
		{
			"description": [
				"Screenshot is the response entity key that holds the recorded value of the most recent screenshot captured by the SaveScreenshot plugin.",
				"When the Base64 switch is not used this value is the absolute file path where the screenshot was saved. When the Base64 switch is supplied the value is a base64-encoded PNG string representing the screenshot content.",
				"This entity allows downstream steps to access the latest screenshot directly from the plugin response, independent of the SaveScreenshot:Screenshots session parameter which accumulates all values across invocations."
			],
			"name": "Screenshot",
			"type": "String"
		}
	],
	"examples": [
		{
			"context": {
				"annotations": {
					"edge_cases": [
						"Directory does not exist — Directory.CreateDirectory is called first and creates it automatically",
						"FileName already ends with .png — no second extension is appended; the name is used as-is",
						"FileName does not end with .png — .png is appended before writing the file",
						"Directory path contains spaces or special characters — Path.Combine handles them; the caller is responsible for otherwise valid path characters",
						"File with the same name already exists in the directory — the existing file is silently overwritten",
						"Session parameter SaveScreenshot:Screenshots is not yet initialised — the plugin defaults to an empty JSON array and starts a new accumulation list"
					],
					"expected_result": "A file named PageScreenshot.png is saved under the Screenshots directory. Its absolute path is appended to the SaveScreenshot:Screenshots session parameter.",
					"notes": "Both Directory and FileName are optional. When both are supplied the plugin uses them directly, appending .png to FileName only if not already present.",
					"use_case": "full_page_screenshot_with_directory_and_filename",
					"version": "1.0"
				},
				"labels": [
					"AuditTrail",
					"BrowserScreenshot",
					"CrossBrowserTesting",
					"CustomDirectory",
					"CustomFileName",
					"FullPage",
					"PngEnforcement",
					"ScreenCapture",
					"SessionOutput",
					"VisualRegression",
					"WebDriver"
				]
			},
			"description": [
				"### Full-page screenshot with custom directory and file name",
				"",
				"Captures a screenshot of the full browser viewport and saves it as `PageScreenshot.png` in the `Screenshots` directory.",
				"The saved absolute path is appended to the `SaveScreenshot:Screenshots` session parameter."
			],
			"rule": {
				"$type": "Action",
				"argument": "{{$ --Directory:Screenshots --FileName:PageScreenshot.png}}",
				"pluginName": "SaveScreenshot"
			}
		},
		{
			"context": {
				"annotations": {
					"edge_cases": [
						"No argument supplied — Directory defaults to Environment.CurrentDirectory and FileName defaults to a new GUID string",
						"Current working directory is read-only — Directory.CreateDirectory does not throw for an existing directory, but the file write fails with UnauthorizedAccessException",
						"Multiple calls in the same session — each generates a unique GUID-named file; all paths accumulate in SaveScreenshot:Screenshots without collision",
						"Session already contains paths in SaveScreenshot:Screenshots — the new path is appended to the existing JSON array, not replacing it",
						"WebDriver session is unexpectedly terminated — the screenshot call throws a WebDriverException before the file is written"
					],
					"expected_result": "A PNG file with a GUID-based name is saved in the current working directory. Its absolute path is appended to the SaveScreenshot:Screenshots session parameter.",
					"notes": "When the plugin is invoked with no argument both Directory and FileName use their defaults. Repeated calls safely accumulate paths in the session parameter.",
					"use_case": "full_page_screenshot_default_no_parameters",
					"version": "1.0"
				},
				"labels": [
					"AutoGeneratedName",
					"BrowserScreenshot",
					"DefaultBehavior",
					"FullPage",
					"GuidFileName",
					"NoParameters",
					"ScreenCapture",
					"SessionAccumulation",
					"SessionOutput",
					"WebDriver"
				]
			},
			"description": [
				"### Full-page screenshot with default settings",
				"",
				"Captures a screenshot of the full browser viewport using all default settings.",
				"This is the simplest invocation — no directory or file name configuration is required."
			],
			"rule": {
				"$type": "Action",
				"pluginName": "SaveScreenshot"
			}
		},
		{
			"context": {
				"annotations": {
					"edge_cases": [
						"Element is not found by the locator — GetElement returns null; the plugin falls back to a full-page screenshot",
						"Element is present in the DOM but not visible — the element screenshot may capture an empty or transparent region",
						"Element is partially outside the viewport — the screenshot captures only the visible portion of the element",
						"Directory does not exist — created automatically before the file is written",
						"FileName already ends with .png — used as-is without appending a second extension",
						"Multiple element screenshots in the same session with different FileName values — each path is appended to SaveScreenshot:Screenshots without collision"
					],
					"expected_result": "A screenshot of the element identified by #ClickButton is saved as ElementScreenshot.png in the Screenshots directory. Its absolute path is appended to the SaveScreenshot:Screenshots session parameter.",
					"notes": "When OnElement and Locator are both provided the plugin calls element.SaveScreenshot, scoping the capture to that element only. If the element cannot be found the plugin silently falls back to a full-page screenshot.",
					"use_case": "element_screenshot_with_css_selector",
					"version": "1.0"
				},
				"labels": [
					"CssSelector",
					"CustomDirectory",
					"CustomFileName",
					"ElementScreenshot",
					"NullElementFallback",
					"PartialCapture",
					"PngEnforcement",
					"ScreenCapture",
					"SessionOutput",
					"TargetedCapture",
					"VisualRegression",
					"WebDriver"
				]
			},
			"description": [
				"### Element-level screenshot with CSS selector",
				"",
				"Captures a screenshot scoped to the element identified by the CSS selector `#ClickButton` and saves it as `ElementScreenshot.png` in the `Screenshots` directory.",
				"When `OnElement` and `Locator` are provided the plugin calls `element.SaveScreenshot` instead of the full-page capture.",
				"If the element is not found by the locator the plugin falls back to a full-page screenshot.",
				"The saved absolute path is appended to the `SaveScreenshot:Screenshots` session parameter."
			],
			"rule": {
				"$type": "Action",
				"argument": "{{$ --Directory:Screenshots --FileName:ElementScreenshot.png}}",
				"locator": "CssSelector",
				"onElement": "#ClickButton",
				"pluginName": "SaveScreenshot"
			}
		},
		{
			"context": {
				"annotations": {
					"edge_cases": [
						"No file is written to disk — Directory and FileName are ignored for the disk write because the Base64 switch skips file output entirely",
						"Session parameter SaveScreenshot:Screenshots already contains values — the base64 string is appended to the existing JSON array, mixing paths and base64 values when earlier calls did not use the switch",
						"OnElement is supplied — the Base64 switch applies equally to element-level captures; the scoped screenshot is encoded as base64 instead of being saved",
						"WebDriver session is unexpectedly terminated — the screenshot call throws a WebDriverException before any value is recorded"
					],
					"expected_result": "No file is written to disk. The full-page screenshot is encoded as a base64 PNG string, appended to the SaveScreenshot:Screenshots session parameter, and exposed as the Screenshot key on the plugin response entity.",
					"notes": "The Base64 switch is self-contained and governs the plugin's behavior directly, independent of the automation-wide screenshot settings. Use it to embed the image in the response entity for downstream processing rather than persisting it to disk.",
					"use_case": "full_page_screenshot_as_base64_response_entity",
					"version": "1.0"
				},
				"labels": [
					"Base64",
					"BrowserScreenshot",
					"EmbeddedImage",
					"FullPage",
					"InMemory",
					"NoDiskWrite",
					"ResponseEntity",
					"ScreenCapture",
					"SessionOutput",
					"WebDriver"
				]
			},
			"description": [
				"### Full-page screenshot returned as base64",
				"",
				"Captures a screenshot of the full browser viewport and returns it as a base64-encoded PNG string instead of writing a file to disk.",
				"When the `--Base64` switch is supplied the plugin skips the disk write and records the base64 content as the value.",
				"The base64 string is appended to the `SaveScreenshot:Screenshots` session parameter and exposed as the `Screenshot` key on the plugin response entity, embedding the image in the response for downstream processing."
			],
			"rule": {
				"$type": "Action",
				"argument": "{{$ --Base64}}",
				"pluginName": "SaveScreenshot"
			}
		}
	],
	"key": "SaveScreenshot",
	"namespace": "G4.Plugins.Ui",
	"outputParameters": [
		{
			"description": [
				"A JSON array of the recorded value for every screenshot captured during the automation session.",
				"Each value is an absolute file path by default, or a base64-encoded PNG string when the Base64 switch is supplied.",
				"Each invocation of SaveScreenshot appends the new value to the existing list, so the parameter accumulates values across multiple calls in the same session.",
				"Use this parameter in downstream steps to validate, move, rename, attach, or embed the captured screenshots."
			],
			"name": "SaveScreenshot:Screenshots",
			"type": "Array"
		}
	],
	"parameters": [
		{
			"default": ".",
			"description": [
				"The directory where the screenshot file is saved.",
				"Defaults to the current working directory when omitted."
			],
			"mandatory": false,
			"name": "Directory",
			"type": "Uri"
		},
		{
			"description": [
				"The name of the screenshot file.",
				"When omitted a new GUID string is used as the file name."
			],
			"mandatory": false,
			"name": "FileName",
			"type": "String"
		},
		{
			"description": [
				"When present, the screenshot is returned as a base64-encoded PNG string instead of being written to disk.",
				"The base64 content is exposed as the `Screenshot` key on the plugin response entity and appended to the `SaveScreenshot:Screenshots` session parameter, allowing the embedded image to be carried inside the response for downstream processing.",
				"Downstream plugins can read the image directly from the response entity without relying on the file system.",
				"When omitted the screenshot is saved to disk and the resolved file path is used instead.",
				"This switch is self-contained: it governs the plugin's behavior directly and does not depend on the automation-wide screenshot settings."
			],
			"mandatory": false,
			"name": "Base64",
			"type": "Switch"
		}
	],
	"pluginType": "Action",
	"properties": [
		{
			"default": "Xpath",
			"description": [
				"The locator strategy used to find the element specified in OnElement.",
				"When OnElement is absent this property is ignored and the plugin takes a full-page screenshot."
			],
			"mandatory": false,
			"name": "Locator",
			"type": "String"
		},
		{
			"description": [
				"A locator expression that identifies the element to screenshot.",
				"When supplied the plugin captures only that element instead of the full browser viewport.",
				"When the element cannot be found the plugin falls back to a full-page screenshot without raising an error."
			],
			"mandatory": false,
			"name": "OnElement",
			"type": "String"
		}
	],
	"protocol": {
		"w3c": "GET /session/{session id}/screenshot",
		"apiDocumentation": "https://www.w3.org/TR/webdriver/#take-screenshot"
	},
	"platforms": [
		"Any"
	],
	"summary": [
		"SaveScreenshot (also accepted as GetScreenshot and TakeScreenshot) captures and saves a PNG screenshot of the full browser viewport or a specific element to disk during automation workflows.",
		"It automatically creates the target directory, enforces the `.png` extension on every output file, generates a unique GUID-based file name when none is provided, and appends every recorded value to the `SaveScreenshot:Screenshots` session parameter while also exposing it as the `Screenshot` key on the plugin response entity for downstream consumption.",
		"When the `--Base64` switch is supplied the plugin skips the disk write and records the screenshot as a base64-encoded PNG string, embedding the image in the response for later processing.",
		"Use this action whenever visual evidence, regression baselines, element-level captures, embedded base64 images, or screenshot audit trails are required in RPA workflows or automated test suites."
	],
	"version": "5"
}
