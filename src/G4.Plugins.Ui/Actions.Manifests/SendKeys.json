{
	"author": {
		"link": "https://www.linkedin.com/in/roei-sabag-247aa18/",
		"name": "Roei Sabag"
	},
	"categories": [
		"WebDriver",
		"Ui"
	],
	"context": {
		"integration": {
			"github": {
				"document": "https://github.com/g4-api/g4-plugins/blob/main/docs/Actions/SendKeys.md",
				"source": "https://github.com/g4-api/g4-plugins/blob/main/src/G4.Plugins.Ui/Actions/SendKeys.cs"
			},
			"rag": {
				"description": "The SendKeys action types text into target UI elements with support for typing delay, clearing options, and keyboard modifier combinations. It targets elements via XPath or CSS locators and handles element-not-found scenarios by logging exceptions.",
				"qa": [
					{
						"question": "What does the SendKeys action do?",
						"answer": "SendKeys locates a target web element and sends a text string to it character by character, with optional per-character delay, pre-clearing, and keyboard modifier combinations."
					},
					{
						"question": "How is the text to send determined?",
						"answer": "The text is taken from the Keys parameter when supplied. When Keys is absent the Argument property value is used directly as the text to type."
					},
					{
						"question": "What parameters does SendKeys support?",
						"answer": "SendKeys supports Keys (the text string to send), Delay (per-character pause in TimeSpan or millisecond format), Clear (standard WebDriver element clear), NativeClear (backspace-simulation clear), and Modifier (one or more keyboard modifier keys such as Control, Alt, Shift, or Meta)."
					},
					{
						"question": "How does element targeting work in SendKeys?",
						"answer": "The target element is identified by the OnElement property evaluated with the strategy specified by Locator. The default locator is Xpath. An optional RegularExpression property narrows element matching on the resolved attribute or text value."
					},
					{
						"question": "When should NativeClear be used instead of Clear?",
						"answer": "Use NativeClear for custom controls such as content-editable divs or inputs with event handlers that ignore the standard WebDriver clear command. NativeClear simulates repeated backspace key presses. Use Clear for standard input elements that respond correctly to the WebDriver element clear method."
					},
					{
						"question": "How are modifier keys specified?",
						"answer": "The Modifier parameter accepts a single value such as Control, or repeated --Modifier parameters that are combined into an array at runtime. All specified modifier keys are held down while the text is sent to the element."
					},
					{
						"question": "What is the manifest version for SendKeys?",
						"answer": "The manifest version is 4."
					},
					{
						"question": "What happens if the target element is not found?",
						"answer": "A NoSuchElementException is wrapped in a G4ExceptionModel, added to the plugin exception list, and the action returns an empty response without sending any text."
					}
				]
			},
			"sequentialWorkflow": {
				"$type": "Action",
				"componentType": "task",
				"iconProvider": {
					"name": "keyboard",
					"svg": null
				},
				"label": "Send Keys",
				"model": "ActionRuleModel"
			},
			"mcp": {
				"title": "Send Keys"
			}
		}
	},
	"description": [
		"### Purpose",
		"",
		"Types text into a target web element such as input fields or text areas with configurable typing speed, content clearing, and keyboard modifier combinations.",
		"The text to send is supplied via the Keys parameter or directly as the Argument value when Keys is absent.",
		"",
		"### Key Features and Functionality",
		"",
		"| Feature              | Description                                                                |",
		"|----------------------|----------------------------------------------------------------------------|",
		"| Text Input           | Sends keystrokes to the element with configurable delay between characters |",
		"| Clear Before Typing  | Supports native backspace clear and standard WebDriver clear               |",
		"| Modifier Keys        | Press Ctrl, Alt, Shift, or Meta while sending text for shortcuts           |",
		"| Flexible Text Source | Accepts text from the Keys parameter or rule argument                      |",
		"",
		"### Usages in RPA",
		"",
		"| Use Case          | Description                                 |",
		"|-------------------|---------------------------------------------|",
		"| Form Field Entry  | Automatically fill text inputs in web forms |",
		"| Search Box Input  | Type search queries into search fields      |",
		"| Text Area Content | Enter multi-line text into text areas       |",
		"",
		"### Usages in Automation Testing",
		"",
		"| Use Case           | Description                                     |",
		"|--------------------|-------------------------------------------------|",
		"| Input Validation   | Test form fields with various text inputs       |",
		"| Keyboard Shortcuts | Test Ctrl+A, Ctrl+C type combinations           |",
		"| Type Speed Testing | Verify typing behavior with configurable delays |"
	],
	"examples": [
		{
			"context": {
				"annotations": {
					"edge_cases": [
						"Element not found",
						"Empty keys value",
						"Keys parameter overrides argument"
					],
					"expected_result": "The text 'Hello World' is typed into the input field.",
					"notes": "The action uses the Keys parameter from the argument macro. When Keys is empty, the argument itself is used as fallback text.",
					"use_case": "basic_text_input",
					"version": "1.0"
				},
				"labels": [
					"Input",
					"TextEntry",
					"Typing"
				]
			},
			"description": [
				"### Send text to an input field",
				"",
				"The rule targets an input element using XPath and sends the text 'Hello World' to it.",
				"The element is located using the default Xpath locator strategy."
			],
			"rule": {
				"$type": "Action",
				"argument": "{{$ --Keys:Hello World}}",
				"locator": "Xpath",
				"onElement": "//input[@id='username']",
				"pluginName": "SendKeys"
			}
		},
		{
			"context": {
				"annotations": {
					"edge_cases": [
						"Element not found",
						"Clear fails silently",
						"NativeClear preferred for custom controls"
					],
					"expected_result": "The input field is cleared first, then 'New Text' is entered.",
					"notes": "NativeClear uses backspace simulation. Standard Clear uses WebDriver Clear method. Only one clear method should be used.",
					"use_case": "clear_before_input",
					"version": "1.0"
				},
				"labels": [
					"Clear",
					"Input",
					"TextEntry"
				]
			},
			"description": [
				"### Clear and type new content",
				"",
				"The rule clears the target element using native backspace simulation before sending the new text.",
				"This is useful when replacing existing content in an input field."
			],
			"rule": {
				"$type": "Action",
				"argument": "{{$ --Keys:New Text --NativeClear}}",
				"locator": "Xpath",
				"onElement": "//textarea[@name='description']",
				"pluginName": "SendKeys"
			}
		},
		{
			"context": {
				"annotations": {
					"edge_cases": [
						"Element not found",
						"Modifier combination fails",
						"Invalid modifier key name"
					],
					"expected_result": "Ctrl+A is pressed to select all text in the element.",
					"notes": "Modifier keys are held down while the text is sent. Multiple modifiers can be provided as a JSON array.",
					"use_case": "keyboard_shortcut_with_modifier",
					"version": "1.0"
				},
				"labels": [
					"KeyboardShortcut",
					"Modifier",
					"Selection"
				]
			},
			"description": [
				"### Send keys with a modifier",
				"",
				"The rule holds the Ctrl modifier while sending 'a' to trigger a Select All keyboard shortcut.",
				"The modifier is provided as a JSON array containing Control."
			],
			"rule": {
				"$type": "Action",
				"argument": "{{$ --Keys:a --Modifier:Control}}",
				"locator": "Xpath",
				"onElement": "//div[@contenteditable='true']",
				"pluginName": "SendKeys"
			}
		},
		{
			"context": {
				"annotations": {
					"edge_cases": [
						"Element not found",
						"Invalid modifier key name",
						"One modifier key is not supported by the active driver"
					],
					"expected_result": "The Control and Alt modifiers are held while the key 'a' is sent to the target element.",
					"notes": "Modifier accepts repeated parameter values that are converted into an array at runtime. The action holds all provided modifiers while sending the key value to the element.",
					"use_case": "send_key_with_multiple_modifiers",
					"version": "1.0"
				},
				"labels": [
					"KeyboardShortcut",
					"Modifier",
					"MultiModifier"
				]
			},
			"description": [
				"### Send a key with multiple modifiers",
				"",
				"The rule holds the Control and Alt modifiers while sending the key `a` to the target element.",
				"The repeated `--Modifier` parameters are converted into an array at runtime and applied together during the key send."
			],
			"rule": {
				"$type": "Action",
				"argument": "{{$ --Keys:a --Modifier:Control --Modifier:Alt}}",
				"locator": "Xpath",
				"onElement": "//div[@contenteditable='true']",
				"pluginName": "SendKeys"
			}
		},
		{
			"context": {
				"annotations": {
					"edge_cases": [
						"Element not found",
						"Invalid TimeSpan format",
						"Zero delay for instant typing"
					],
					"expected_result": "The text is typed character by character with a 100ms delay between each keystroke.",
					"notes": "Delay accepts TimeSpan format like 00:00:00.100 for 100ms between characters. Default is zero delay for instant typing.",
					"use_case": "typed_input_with_delay",
					"version": "1.0"
				},
				"labels": [
					"Delay",
					"Input",
					"TypingSpeed"
				]
			},
			"description": [
				"### Type text with character delay",
				"",
				"The rule sends text to an element with a 100ms delay between each keystroke.",
				"This simulates human typing speed for applications that require realistic input timing."
			],
			"rule": {
				"$type": "Action",
				"argument": "{{$ --Keys:Slow Type --Delay:00:00:00.100}}",
				"locator": "Xpath",
				"onElement": "//input[@id='search']",
				"pluginName": "SendKeys"
			}
		}
	],
	"key": "SendKeys",
	"namespace": "G4.Plugins.Ui.Actions",
	"parameters": [
		{
			"description": [
				"Clear triggers a standard WebDriver clear operation before sending keys.",
				"It removes existing content from input fields when present.",
				"Use NativeClear for custom controls that do not respond to standard clear."
			],
			"mandatory": false,
			"name": "Clear",
			"type": "Switch"
		},
		{
			"description": [
				"Delay sets the time interval between each keystroke.",
				"Accepts TimeSpan format like 00:00:00.100 for 100ms delay between characters.",
				"Set to 00:00:00 for instant typing without any delay."
			],
			"mandatory": false,
			"name": "Delay",
			"type": "String"
		},
		{
			"description": [
				"Keys specifies the text string to send to the element.",
				"This is the primary way to provide input text via parameter macro syntax.",
				"When empty, the rule argument is used as the text to send."
			],
			"mandatory": false,
			"name": "Keys",
			"type": "String"
		},
		{
			"description": [
				"Modifier specifies one or more keyboard modifier keys to hold while the key value is sent to the target element.",
				"It accepts repeated parameter values that are converted into an array at runtime, such as `--Modifier:Control --Modifier:Alt`.",
				"Use Modifier for keyboard combinations like Control+A, Shift+Tab, or multi-modifier shortcuts."
			],
			"mandatory": false,
			"name": "Modifier",
			"type": "Array"
		},
		{
			"description": [
				"NativeClear triggers a native backspace-based clear before sending keys.",
				"It provides better compatibility with custom input controls.",
				"Use this instead of Clear for elements that do not respond to standard clearing."
			],
			"mandatory": false,
			"name": "NativeClear",
			"type": "Switch"
		}
	],
	"platforms": [
		"Any"
	],
	"pluginType": "Action",
	"properties": [
		{
			"description": [
				"Argument provides the keystroke text as a fallback when the Keys parameter is not set.",
				"It allows passing text directly to the element without using a named parameter.",
				"When both Argument and Keys parameter are present, Keys takes precedence."
			],
			"mandatory": true,
			"name": "Argument",
			"type": "String"
		},
		{
			"description": [
				"Locator determines the strategy for finding the target element.",
				"It defaults to Xpath for element location.",
				"Supported locators include Xpath, CssSelector, Id, LinkText, and PartialLinkText."
			],
			"default": "Xpath",
			"mandatory": false,
			"name": "Locator",
			"type": "String"
		},
		{
			"description": [
				"OnElement specifies the target element for sending keystrokes.",
				"It is required for identifying which element receives the keyboard input.",
				"The value format depends on the selected locator strategy."
			],
			"mandatory": true,
			"name": "OnElement",
			"type": "String"
		},
		{
			"description": [
				"RegularExpression filters element matching using pattern matching on element attributes.",
				"It defaults to '(?si).*' to match all elements.",
				"Use this to narrow down element selection when multiple elements match the locator."
			],
			"default": "(?si).*",
			"mandatory": false,
			"name": "RegularExpression",
			"type": "String"
		}
	],
	"protocol": {
		"apiDocumentation": "https://www.w3.org/TR/webdriver/#element-send-keys",
		"w3c": "POST /session/{session id}/element/{element id}/value"
	},
	"summary": [
		"SendKeys sends keystrokes to a target element in the UI, supporting configurable delay between characters, pre-clearing via native backspace or standard WebDriver clear, and keyboard modifier combinations.",
		"The Keys parameter supplies the text to type; when absent the Argument value is used as a fallback.",
		"In RPA it automates text entry in forms, search boxes, and text areas; in automation testing it verifies input field behavior, keyboard shortcuts, and typing-speed scenarios."
	],
	"version": "4"
}
